/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/MapleBridge.github.io',
  assetPrefix: '/MapleBridge.github.io/',
  images: {
    unoptimized: true,
  },
}

export default nextConfig